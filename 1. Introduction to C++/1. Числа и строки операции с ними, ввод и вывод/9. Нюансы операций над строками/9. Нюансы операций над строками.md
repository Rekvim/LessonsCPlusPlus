## Нюансы операций над строками

В прошлых уроках вы узнали, что в C++ рекомендуется использовать `""s`-литералы при работе со строками. В этом уроке вы познакомитесь с ситуациями, когда такие литералы защищают ваш код от ошибок.

### Конкатенация строковых 

Вы уже знаете, что для конкатенации строк применяется операция `+`. Она прекрасно работает с переменными, даже когда они проинициализированы обычными строковыми литералами — без `s` после кавычек:
```cpp
#include <iostream>
using namespace std;

int main() {
    string i_know = "I know "; // Используется обычный строковый литерал
    string cpp = "C++";        // Используется обычный строковый литерал
    cout << i_know + cpp << endl;
}
```
Однако использовать `+` для сложения обычных строковых литералов не выйдет:
```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "I know " + "C++" << endl;
}
```
Компилятор выдаст сообщение об ошибке:

prog.cc: In function 'int main()':
prog.cc:5:23: error: invalid operands of types 'const char [8]' and 'const char [4]' to binary 'operator+'
    5 |     cout << "I know " + "C++" << endl;
      |             ~~~~~~~~~ ^ ~~~~~
      |             |           |
      |             |           const char [4]
      |             const char [8]

Дело в том, что операцию `+` нельзя использовать для сложения типов `const char [8]` и `const char [4]`. Строковые литералы `"I know "` и `"C++"` — это строковые литералы языка C или C-строки. Компилятор превращает их в массивы символов, которые дополняются символом с кодом 0. Вот почему размер массива на единицу больше количества символов внутри кавычек. Как бы то ни было, массивы символов — это не строки и операции `+` у них нет.

Чтобы исправить ошибку, используйте `""s`-литералы, которые напрямую создают объект типа `string`. Когда тип хотя бы одного из слагаемых — `string`, операция сложения будет работать как надо:

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "I know "s + "C++"s << endl;
    // Следующие варианты также возможны
    // cout << "I know"s + "C++" << endl;
    // cout << "I know" + "C++"s << endl;
}
```

У строковых литералов языка C есть своя область применения, но их возможности ограничены. К тому же строковые литералы требуют от разработчика больше внимания.

Отметьте правильные способы записать `"Hello, friend!"` в переменную `sum`:

Правильный ответ
```
string x = "Hello, ";
string sum = x + "friend!"; 
```
Да! Строки можно складывать с обычными литералами.

Правильный ответ

Строки можно складывать с обычными литералами.

```
string sum = "Hello, "s + "friend!"s 
```

Верно, можно складывать друг с другом литералы, заканчивающиеся на `s`, — они не отличаются от переменных типа `string`.

Не правильный ответ
```
string sum = "Hello, " + "friend!" 
```
Простые литералы друг с другом складывать нельзя.

Правильный ответ
```
string sum = "Hello, ";
sum += "friend!"; 
```

Правильный ответ
```
string sum = "Hello, "s;
sum += "friend!"s;
```
Строке можно присваивать литерал, заканчивающийся на `s`. Присоединять тоже можно — такие литералы не отличаются от переменных типа `string`.

### Сложение строк и чисел

JavaScript, Java, C# и ряд других языков разрешают складывать строки и числа. При этом число переводится в строковое представление, происходит склейка строк:
```cpp
// Этот код на JavaScript выведет строку Hello1
console.log("Hello" + 1);
```
В C++ складывать строки и числа нельзя:

```cpp
string s = "Hello"s;
string error = s + 1; // ошибка! 
```

Выскочит длинное сообщение об ошибке:

```
no match for 'operator+' (operand types are 'std::string' {aka 'std::__cxx11::basic_string<char>'} and 'int')
    6 |     string error = s + 1; // ошибка!
      |                    ~ ^ ~
      |                    |   |
      |                    |   int
      |                    std::string {aka std::__cxx11::basic_string<char>}
... ещё несколько десятков строк о том, какие возможные 
... типы аргументов операции сложения могли бы подойти, но не подошли
```

Строки и числа — разные типы, и для них операция `+` выполняет совершенно разные действия: конкатенацию строк и сложение чисел. В JavaScript если один из аргументов операции сложения — строка, другой аргумент неявно преобразовывается в строку. Потом происходит конкатенация строк.

В C++ преобразование числа в строку и обратно должно выполняться явно. Для этого служат функции:

- stoi;
- stod;
- `to_string`.
  
```cpp
int x = stoi(x_str);      // string -> int
double y = stod(x_str);   // string -> double
string s = to_string(x);  // int or double -> string

int z = 42;
string t = "Hello"s + to_string(z); // В t будет значение "Hello42" 
```

Функции `stoi` и `stod` завершатся с ошибкой, если переданную им строку нельзя преобразовать в целое или вещественное число. Такие ошибки надо правильно обработать, иначе программа аварийно завершит работу. Как именно их обрабатывать, узнаете в будущих уроках.

При сложении строк и чисел возникает ошибка компиляции. В частности, это происходит при сложении ""s-литералов и целых чисел. ""s-литералы защищают от случайных ошибок, которые присущи обычным строковым литералам.

C++ унаследовал от языка C возможность складывать простые строковые литералы с числами. При этом код компилируется без ошибок, но при запуске получается непривычный результат, который даже может привести к краху программы:
```cpp
string s = "Hello" + 1; // У переменной s будет значение "ello"
string q = 1 + "Hello"; // У переменной q тоже будет значение "ello"

// Здесь поведение программы будет неопределённым, 
// так как числовой аргумент превышает длину строки:
//string e = "Hello" + 10000; 
```
Чтобы использовать строковые литералы так, надо хорошо понимать модель памяти C++. Иначе возможны ошибки и нестабильная работа программы. Модель памяти C++ вы будете изучать далее в курсе. А пока при работе со строками всегда используйте `""s`-литералы. Они защитят ваш код от сюрпризов.

### Размер строки

Узнать размер строки позволит функция `size`. Синтаксис её вызова: сначала идёт объект строки, потом через точку записывается имя функции:
```cpp
// Метод size вызывается у строки, созданного строковым литералом "Hello"s
int length = "Hello"s.size(); // значение length равно 5

string greeting = "Hi"s;
// Метод size вызывается у строки greeting
int greeting_size = greeting.size(); // значение greeting_size равно 2
greeting = "Bye"s;
greeting_size = greeting.size(); // значение greeting_size 3 
```

Такие функции называют функциями-членами, или методами. Подробнее о методах — далее в курсе.

Размер строки, которую вернул метод `size`, исчисляется в элементах типа `char`. Это не обязательно то же самое количество графических знаков, которые увидит пользователь:
```
cout << "Hello"s.size() << endl // 5
     << "Привет"s.size() << endl // 12 — кириллица в UTF-8
     << "👋🏾"s.size() << endl; // 8 — эмодзи 
```

Если строка состоит только из латинских букв, цифр, пробела и основных знаков препинания, её размер будет равен её длине. Поэтому мы часто будем говорить о длине строки, подразумевая её размер.

Благодаря стандарту Юникод есть минимум четыре определения длины строки. В разных языках программирования под длиной строки могут подразумеваться разные понятия. Сравните длину строки "👋🏾" в C++, Java, Python и Swift.

- C++: восемь байт в кодировке UTF-8;
- Java: четыре двухбайтных элемента в кодировке UTF-16;
- Python: два четырехбайтных элемента в кодировке UTF-32;
- Swift: один графемный кластер — символ в понимании человека.

Похожая ситуация с символами кириллицы и иероглифами. Поэтому в XXI веке «длина строки» — неоднозначное понятие.

Пусть `number` — число типа `int`, `digits` — строка. Отметьте выражения, которые скомпилируются:

Неправильнй ответ
`string x = "Hello, citizen #" << number;`

Оператор `<<` применяют для записи в поток, а здесь слева от него стоит строка.

Правильный ответ
`string x = "Hello, citizen #" + to_string(number);`
Строковый литерал складывается со строкой, это дозволенная операция.

Правильный ответ
`double x = stoi(digits);`
Строка преобразуется в `int`, а затем результат неявно преобразуется к `double`.

Неправильный ответ
`double x = stod(number);`
`stod` ожидает получить на вход строку, а получает `int`.

Тоже правильный ответ
`to_string(stoi(digits)) == digits;`
Строка преобразуется в число и обратно в строку, строку можно сравнивать со строкой.

Тоже правильный ответ
`stod(digits) < stoi(digits);`
double можно сравнивать с `int` (произойдёт неявное преобразование).

`digits == number;`
Строку нельзя сравнивать с числом.