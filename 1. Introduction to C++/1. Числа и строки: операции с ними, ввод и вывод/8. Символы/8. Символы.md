## Символы

В прошлом уроке вы познакомились со строками. Строки позволяют программе работать с текстовыми данными: именами людей и кличками животных, содержимым текстовых документов, адресами электронной почты и веб-страниц. Строки в C++ представлены типом `string`. Чтобы его использовать, нужно подключить библиотеку `<string>`. В этом уроке вы познакомитесь с элементами строк — символами.

Строка — произвольная последовательность элементарных единиц, символов алфавита. В `string` каждый элемент имеет тип `char`. На большинстве современных платформ `char` состоит из восьми бит и может иметь всего $2^8 =256$ различных значений. Этого достаточно, чтобы представить символы английского алфавита, цифры, знаки препинания и ряд служебных символов.

Для представления символов кириллицы, ближневосточных языков, иероглифов и эмодзи одного `char` уже не достаточно. В зависимости от используемой кодировки может потребоваться два и более `char`. В заданиях и примерах Практикума почти всегда будут использоваться только те символы, для которых достаточно одного элемента `char`.

В C++ тип `char` хранит число, равное коду символа. Например, заглавная буква `A` английского алфавита на большинстве платформ имеет код символа, равный 65. Для представления символов часто используются символьные литералы, в которых символ заключается в одинарные кавычки. Так код становится более понятным:

```cpp
char letter_a = 'A';    // Сразу понятно, что letter_a хранит код символа A
char letter_a_too = 65; // Тоже хранит код символа A, но это не очевидно

char letter_b = letter_a + 1; // Буква B имеет код, следующий за кодом символа A 
```

Для доступа к отдельным символам строки определена операция индексации. Чтобы обратиться к символу строки, нужно указать его номер — индекс — внутри квадратных скобок `[]`. Индексация элементов строк и массивов в C++ начинается с нуля. Следом за последним символом строки находится служебный символ с кодом 0. Он нужен для совместимости со строками языка C.

```cpp
string greeting = "Hello"s;
char ch = greeting[1];
cout << ch; // Выведет символ e
```
Оператор индексации можно использовать и для изменения символов строки:
```
// В greeting было слово Hello
greeting[0] = 'Y'; // Теперь в greeting содержится  "Yello" 
```
Будьте внимательны. Индекс символа должен быть в диапазоне [0; длина строки). Если указать индекс за пределами этого диапазона, поведение программы будет неопределённым.

Операции `+` и `+=` могут приклеивать к строке не только строки, но и одиночные символы:

```cpp
// В greeting было слово Yello
greeting += 'w';   // Внутри greeting строка "Yellow"
cout << greeting << endl;
```

Тип `char` хранит не строку, а код символа. Поэтому, если сложить два значения типа `char`, произойдёт не конкатенация символов в строку, а сложение кодов символов. В итоге получится число:

```cpp
cout << 'A' + 'B' << endl; // Выведет число 131, равное сумме 65 и 66 
```
Если нужна конкатенация, создайте сначала строку из одного символа, а затем используйте операцию `+`:

```cpp
// string(1, 'A') создаёт строку, состоящую из одного символа A
cout << string(1, 'A') + 'B' << endl; // Выведет AB

// Подобным образом можно создать строку из нескольких одинаковых символов:

cout << string(3, 'A') + 'B' << endl; // Выведет AAAB 
```

Как сказано чуть выше, многие символы за пределами английского алфавита кодируются последовательностью из нескольких байт. Но C++ придумали до распространения таких кодировок. Поэтому в нём под элемент типа `char` всегда выделяется один байт. Этого хватит на букву латинского алфавита, а на эмодзи и кириллицу — уже нет. Например, создать символ, содержащий котика, не получится:

```cpp
#include <iostream>

using namespace std;

int main() {
    cout << '🐈' << endl;
}
```
Компилятор его увидит и укажет на ошибку:
```
cat.cpp:6:13: error: multi-character character constant [-Werror=multichar]
     cout << '🐈' << endl;
             ^~~~~~ 
```
Зато котика можно посадить в строку:
```cpp
#include <iostream>

using namespace std;

int main() {
    cout << "🐈"s << endl;
} 
```
Для обработки подобных символов есть специальные библиотеки, но в этом курсе они не понадобятся.